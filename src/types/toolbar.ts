/**
 * Button Types
 */
export enum ButtonType {
  DOWNLOAD = 'download',
  NAVIGATION = 'navigation',
  ZOOM = 'zoom',
}

/**
 * Zoom Types
 */
export enum ZoomType {
  DEFAULT = 'default',
  PANPINCH = 'panpinch',
}
