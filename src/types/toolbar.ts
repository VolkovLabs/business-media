/**
 * Button Types
 */
export enum ButtonType {
  AUTOPLAY = 'autoplay',
  DOWNLOAD = 'download',
  NAVIGATION = 'navigation',
  ZOOM = 'zoom',
}

/**
 * Zoom Types
 */
export enum ZoomType {
  DEFAULT = 'default',
  PANPINCH = 'panpinch',
}
